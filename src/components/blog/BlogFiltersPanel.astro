---
import Blogfilters from "./BlogFilters.vue";
const categories = [
    "Aventura",
    "Cultura",
    "Naturaleza",
    "Experiencia",
    "Gastronomía",
    "Relajación",
    "Romance",
    "Fotografía",
    "Otro",
];

// Aesthetic solid colors for category tags when active
const CATEGORY_COLORS: Record<string, string> = {
    Aventura: "#f59e0b", // amber
    Cultura: "#8b5cf6", // purple
    Naturaleza: "#22c55e", // green
    Experiencia: "#eab308", // yellow
    Gastronomía: "#ef4444", // red
    Relajación: "#0ea5e9", // light blue
    Romance: "#db2777", // pink
    Fotografía: "#64748b", // slate
    Otro: "#94a3b8",
};

const currentCategory = Astro.url.searchParams.get("category") || "todas";
---

<div id="blog-filtros-wrapper">
    <Blogfilters client:load categories={categories} categoryColors={CATEGORY_COLORS} currentCategory={currentCategory} />
</div>

<script>
    </script>