---
// Icon.astro - Helper component to render SVG sprites
interface Props {
    name: string;
    class?: string;
}
const { name, class: className = "w-4 h-4" } = Astro.props;
---
<svg class={className} aria-hidden="true" focusable="false">
    <use href={`#icon-${name}`}></use>
</svg>
