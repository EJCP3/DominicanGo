---
// ProvinceModal.astro — Mounts the Vue ProvinceModal component.
// Using client:only="vue" to avoid hydration mismatches for this client-side-only modal.
import ProvinceModalVue from './ProvinceModal.vue';
---

<ProvinceModalVue client:only="vue" />
