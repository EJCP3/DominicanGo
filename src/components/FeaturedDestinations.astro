---
// Featured destinations grid — populated by JS with province data
---

<section id="destinos" class="py-16 sm:py-24 bg-ocean-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h3 class="font-heading font-bold text-3xl sm:text-4xl mb-4">
                Destinos Destacados
            </h3>
            <p class="text-ocean-300 max-w-xl mx-auto">
                Las provincias más visitadas y sus experiencias más memorables
            </p>
        </div>
        <div
            class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6"
            id="featured-grid"
        >
            <!-- Populated by JS -->
        </div>
    </div>
</section>
